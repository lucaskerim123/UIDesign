const supportText='Need support or just a quick chat? Email us at support@orbitfs.cc or submit a ticket through the Customer Portal.';
const ownerText='OrbitFS is owned and operated by IncendiaryNetworks Inc.';
function esc(v:string){return String(v||'').replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'\"':"&quot;"}[c]||c))}
export function wrapOrbitFsText(body:string){return `${supportText}\n\n${String(body||'').trim()}\n\n${ownerText}`}
export function wrapOrbitFsHtml(body:string){const raw=String(body||'').trim();if(/^<!doctype\s+html|^<html[\s>]/i.test(raw))return raw;const safe=raw&&/<[a-z][\s\S]*>/i.test(raw)?raw:`<p>${esc(raw).replace(/\n\n/g,'</p><p>').replace(/\n/g,'<br>')}</p>`;return `<!doctype html><html><body style="margin:0;background:#f4f7fb;font-family:Arial,sans-serif;color:#172033"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="padding:28px 14px;background:#f4f7fb"><tr><td align="center"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:680px;background:#fff;border:1px solid #e3e8f0;border-radius:14px;overflow:hidden"><tr><td style="padding:12px 24px;background:#eef4fb;color:#38526f;font-size:12px;line-height:1.55">Need support or just a quick chat? Email us at <a href="mailto:support@orbitfs.cc" style="color:#1d5ea8">support@orbitfs.cc</a> or submit a ticket through the Customer Portal.</td></tr><tr><td style="padding:22px 28px;background:#111827;color:#fff;font-size:22px;font-weight:700">OrbitFS</td></tr><tr><td style="padding:28px;font-size:15px;line-height:1.7;color:#374151">${safe}</td></tr><tr><td style="padding:16px 28px;border-top:1px solid #edf0f5;color:#6b7280;font-size:12px;text-align:center">${ownerText}</td></tr></table></td></tr></table></body></html>`}

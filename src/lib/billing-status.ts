export function billingLabel(order:any,invoice?:any){if(order?.status==='terminated'||invoice?.status==='cancelled'||invoice?.status==='void')return 'Canceled / Terminated';if(order?.status==='suspended')return 'Suspended / Suspended';if(order?.payment_status==='paid_admin_override')return 'APAID / Active';if(order?.payment_status?.startsWith('paid')||invoice?.status==='paid')return 'Paid / Active';if(order?.payment_status==='overdue'||invoice?.status==='overdue')return 'Overdue / Pending Payment';return 'Unpaid / Pending Payment'}
export function invoiceLabel(invoice:any){if(invoice?.status==='paid')return 'Paid';if(invoice?.status==='overdue')return 'Overdue';if(invoice?.status==='cancelled'||invoice?.status==='void')return 'Canceled';if(invoice?.status==='partial')return 'Partially paid';return 'Unpaid'}

"use client";
import {useEffect,useMemo,useState} from "react";import {createClient} from "@/lib/supabase";import Link from "next/link";import "../licensing/licensing.css";
export default function AdminReleaseManager(){const sb=useMemo(()=>createClient(),[]),[releases,setReleases]=useState<any[]>([]),[loading,setLoading]=useState(true),[msg,setMsg]=useState(""),[busy,setBusy]=useState(""),[draft,setDraft]=useState({component:"orbitfs_base",version:"",channel:"stable",changelog:""});async function request(path:string,method="GET",body?:any){const {data:{session}}=await sb.auth.getSession();if(!session?.access_token)throw Error("Administrator session expired.");const r=await fetch(`/api/admin/license-master?path=${encodeURIComponent(path)}`,{method,headers:{Authorization:`Bearer ${session.access_token}`,...(body?{"Content-Type":"application/json"}:{})},body:body?JSON.stringify(body):undefined});const j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||`License Master returned ${r.status}`);return j}async function load(){setLoading(true);try{const j=await request("/api/v1/releases");setReleases(j.releases||[]);setMsg("")}catch(e:any){setMsg(e.message||"Could not load releases.")}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function action(id:string,op:string){if(!confirm(`${op} release ${id}?`))return;setBusy(id+op);try{await request(`/api/v1/releases/${encodeURIComponent(id)}/${op}`,"POST",{});setMsg(`Release ${op} completed.`);await load()}catch(e:any){setMsg(e.message||`Release ${op} failed.`)}finally{setBusy("")}}async function createDraft(){if(!draft.version||!draft.changelog)return setMsg("Version and changelog are required.");setBusy("create");try{await request("/api/v1/releases","POST",draft);setMsg("Release draft created.");setDraft({...draft,version:"",changelog:""});await load()}catch(e:any){setMsg(e.message||"Could not create release draft.")}finally{setBusy("")}}return <main className="lmPage"><div className="lmHero"><div><div className="lmEyebrow">RELEASE / UPDATE SYSTEM</div><h1>Release Manager</h1><p>Draft, validate, publish and control releases through License Master. Publishing never silently deploys customers.</p></div><div className="lmHeroActions"><Link href="/admin/licensing">License Master</Link><Link href="/admin/license-controller">License Controller</Link><button onClick={()=>void load()}>Refresh</button></div></div><div className="lmGrid2"><section className="lmCard"><header><div><div className="lmKicker">CREATE</div><h2>Draft release</h2><p>The changelog becomes customer-facing release information.</p></div></header><div className="lmKV"><label>Component<select value={draft.component} onChange={e=>setDraft({...draft,component:e.target.value})}><option value="orbitfs_base">orbitfs_base</option><option value="orbitfs_mcp">orbitfs_mcp</option><option value="orbitfs_apex">orbitfs_apex</option><option value="orbitfs_studio">orbitfs_studio</option></select></label><label>Version<input value={draft.version} onChange={e=>setDraft({...draft,version:e.target.value})} placeholder="2.0.1"/></label><label>Channel<select value={draft.channel} onChange={e=>setDraft({...draft,channel:e.target.value})}><option>stable</option><option>beta</option><option>preview</option></select></label><label>Changelog<textarea rows={9} value={draft.changelog} onChange={e=>setDraft({...draft,changelog:e.target.value})} placeholder="Added…\nFixed…\nChanged…"/></label></div><button className="primary" disabled={busy==="create"} onClick={()=>void createDraft()}>{busy==="create"?"Creating…":"Create draft"}</button></section><section className="lmCard"><header><div><div className="lmKicker">LIFECYCLE</div><h2>Draft → validate → publish</h2></div></header><div className="lmRuntimeList"><div><b>Draft</b><span>Version and changelog</span></div><div><b>Validate</b><span>Artifact validation gate</span></div><div><b>Publish</b><span>Eligible for Billing/customer deployment</span></div><div><b>Deploy</b><span>Customer-triggered update</span></div></div></section></div><section className="lmCard"><header><div><div className="lmKicker">RELEASES</div><h2>Master release catalogue</h2></div></header>{loading?<div className="lmLoading">Loading…</div>:<div className="lmReleaseList">{releases.map((r:any)=><div className="lmRelease" key={r.id}><div><b>{r.version||r.id}</b><small>{r.component||r.product_code||"OrbitFS"} · {r.channel||"stable"} · {r.status||"draft"}</small><p style={{whiteSpace:"pre-wrap"}}>{r.changelog||r.release_notes||"No changelog"}</p></div><div className="actions"><button disabled={!!busy} onClick={()=>void action(r.id,"validate")}>Validate</button><button className="primary" disabled={!!busy} onClick={()=>void action(r.id,"publish")}>Publish</button><button disabled={!!busy} onClick={()=>void action(r.id,"pause")}>Pause</button><button className="danger" disabled={!!busy} onClick={()=>void action(r.id,"withdraw")}>Withdraw</button></div></div>)}</div>}{msg&&<p>{msg}</p>}</section></main>}

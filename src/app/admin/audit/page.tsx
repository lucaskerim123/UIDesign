"use client";
import {useEffect,useState} from "react";import {createClient} from "@/lib/supabase";
export default function Audit(){const sb=createClient();const [rows,setRows]=useState<any[]>([]);useEffect(()=>{sb.from("admin_audit_log").select("*").order("created_at",{ascending:false}).limit(200).then(({data})=>setRows(data||[]))},[]);return <main className="adminShell"><header className="adminTop"><div><p className="eyebrow">MASTER ADMIN</p><h1>Audit log</h1></div></header><section className="panel"><div className="panelTitle"><h2>Administrative activity</h2><span>{rows.length} latest events</span></div>{rows.map(r=><div className="adminItem" key={r.id}><div><b>{r.action||r.event_type||"Admin action"}</b><span>{r.detail||r.reason||JSON.stringify(r.metadata||{})}</span></div><span>{r.created_at?new Date(r.created_at).toLocaleString():""}</span></div>)}</section></main>}
